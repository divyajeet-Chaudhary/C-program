#include <stdio.h>
int main ()
{
 //calculator
  int a = 100, b = 10;
  int sum = a + b;
  int sub = a - b;
  int mul = a * b;
  int divd = a / b;
  int mod = a % b;
  printf ("the sum is %d\n",sum);
  printf("the sub is %d\n",sub);
  printf ("the mul is %d\n",mul);
  printf("the divd is %d\n",divd);
  printf("the mod is %d\n",mod);
  return 0;
}
